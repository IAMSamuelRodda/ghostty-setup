# Git LFS for large binary files
# Uncomment the line below if you want to use Git LFS for the Ghostty binary
# bin/ghostty-x86_64-linux filter=lfs diff=lfs merge=lfs -text

# Mark shell scripts as executable
*.sh text eol=lf

# Documentation
*.md text eol=lf

# Configuration files
config/** text eol=lf
