# Ghostty Terminal Configuration - Matrix Hacking Vibe

# ===== FONT SETTINGS =====
font-family = JetBrains Mono
font-size = 11

# ===== THEME =====
# Switch themes by changing the include path below:
# - themes/matrix-dramatic  (Current Matrix theme)
# - themes/default-dark     (Standard dark theme)
theme = matrix-dramatic

# ===== WINDOW SETTINGS =====
window-padding-x = 4
window-padding-y = 4
background-opacity = 0.9

# Client-side decorations for thin titlebar (like Kitty)
window-decoration = client
window-theme = ghostty

# ===== CURSOR SETTINGS =====
cursor-style = block
cursor-style-blink = true

# ===== TAB SETTINGS =====
# Note: Ghostty doesn't have powerline tabs, but you can customize appearance
window-inherit-working-directory = true
window-inherit-font-size = true

# ===== MATRIX RAIN COMMANDS =====
# NOTE: Ghostty doesn't support launching external programs via keybindings
# Use shell aliases instead (added to your .bashrc)
#
# Usage: Just type these commands in ghostty:
#   matrix        - Neo with gaps & varied speed (recommended)
#   matrix-dense  - Dense, faster rain
#   matrix-sparse - Very sparse & slow
#   matrix-chaos  - Chaotic with heavy glitching
#   matrix-calm   - Calm, minimal, no glitch
#   matrix-life   - Conway's Game of Life
#   matrix-fract  - Mandelbrot fractals
#   matrix-cxx    - CXXMatrix classic
#   matrix-uni    - UniMatrix classic
#
# Press Ctrl+C to exit matrix rain effects

# ===== TAB MANAGEMENT =====
keybind = ctrl+shift+t=new_tab
keybind = ctrl+shift+w=close_surface
keybind = ctrl+shift+right=next_tab
keybind = ctrl+shift+left=previous_tab

# ===== TEXT SELECTION =====
# Shift+Arrow keys to adjust/extend an existing selection (started with mouse)
# These only work when text is already selected - otherwise keys pass through to shell
keybind = shift+left=adjust_selection:left
keybind = shift+right=adjust_selection:right
keybind = shift+up=adjust_selection:up
keybind = shift+down=adjust_selection:down
keybind = shift+home=adjust_selection:beginning_of_line
keybind = shift+end=adjust_selection:end_of_line
keybind = shift+page_up=adjust_selection:page_up
keybind = shift+page_down=adjust_selection:page_down

# ===== COPY/PASTE =====
keybind = ctrl+shift+c=copy_to_clipboard
keybind = ctrl+shift+v=paste_from_clipboard

# ===== COMMAND LINE EDITING =====
# Word deletion shortcuts (mirrors GUI text editor behavior)
# Delete previous word (Ctrl+W)
keybind = ctrl+backspace=text:\x17
# Delete next word (Alt+D)
keybind = ctrl+delete=text:\x1b\x64

keybind = shift+enter=text:\x1b\r
